# 生产专用，与 compose.yml 互斥；镜像由 CI 预构建 load，不在服务器构建
name: blog

services:
  rustfs:
    image: docker.io/rustfs/rustfs:latest
    container_name: blog_rustfs
    restart: unless-stopped
    command: /data
    environment:
      RUSTFS_ACCESS_KEY: ${RUSTFS_ACCESS_KEY_ID}
      RUSTFS_SECRET_KEY: ${RUSTFS_SECRET_ACCESS_KEY}
      RUSTFS_CONSOLE_ENABLE: "true"
    volumes:
      - rustfs_data:/data
    ports:
      - "9000:9000"
      - "9001:9001"
    networks:
      - blog_net

  postgres:
    image: docker.io/library/postgres:18.3
    container_name: blog_postgres
    restart: unless-stopped
    environment:
      POSTGRES_USER: ${POSTGRES_USER}
      POSTGRES_PASSWORD: ${POSTGRES_PASSWORD}
      POSTGRES_DB: ${POSTGRES_DB}
      POSTGRES_HOST_AUTH_METHOD: md5
    command: -c listen_addresses='*'
    volumes:
      - pgdata:/var/lib/postgresql
    ports:
      - "127.0.0.1:5432:5432"
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U ${POSTGRES_USER} -d ${POSTGRES_DB}"]
      interval: 10s
      timeout: 5s
      retries: 10
    networks:
      - blog_net

  redis:
    image: docker.io/library/redis:latest
    container_name: blog_redis
    restart: unless-stopped
    command: ["redis-server", "--appendonly", "yes"]
    volumes:
      - redis_data:/data
    ports:
      - "127.0.0.1:6379:6379"
    networks:
      - blog_net

  migration:
    # 复用 CI 预构建的 backend 镜像，仅替换启动命令跑迁移
    image: blog_backend:latest
    container_name: blog_migration
    restart: "no"
    command: ["/app/migration"]
    environment:
      # 配置模板里 redis.host 写死为 localhost，用环境变量覆盖为服务名
      APP_REDIS_HOST: redis
    volumes:
      - ./deploy/runtime/backend/config.yaml:/app/config.yml:ro
    depends_on:
      postgres:
        condition: service_healthy
      redis:
        condition: service_started
    networks:
      - blog_net

  backend:
    image: blog_backend:latest
    container_name: blog_backend
    restart: unless-stopped
    environment:
      # 配置模板里 redis.host 写死为 localhost，用环境变量覆盖为服务名
      APP_REDIS_HOST: redis
    volumes:
      - ./deploy/runtime/backend/config.yaml:/app/config.yml:ro
    depends_on:
      postgres:
        condition: service_healthy
      redis:
        condition: service_started
      rustfs:
        condition: service_started
    networks:
      - blog_net

  frontend:
    image: blog_frontend:latest
    container_name: blog_frontend
    restart: unless-stopped
    environment:
      NODE_ENV: production
      NUXT_TELEMETRY_DISABLED: "1"
      # SSR 服务端访问后端地址（同网络用服务名，不设置默认 localhost:8000）
      NUXT_BACKEND_URL: "http://backend:8000"
    # 不依赖 backend，避免其重建阻塞删除；就绪后由 nginx reload 接入
    networks:
      - blog_net

  nginx:
    image: docker.io/library/nginx:latest
    container_name: blog_nginx
    restart: unless-stopped
    ports:
      - "${HTTP_PORT}:80"
      - "${HTTPS_PORT}:443"
    volumes:
      - ./deploy/runtime/nginx/default.conf:/etc/nginx/conf.d/default.conf:ro
      # 须挂载整个 etc（live 软链指向 archive），否则证书加载失败
      - ./deploy/letsencrypt/etc:/etc/letsencrypt:ro
      # ACME HTTP-01 挑战目录，certbot 续期写入、nginx 对外服务
      - ./deploy/acme:/var/www/certbot:ro
    # 不设 depends_on：否则 up -d nginx 会拉起未载入镜像的 frontend/backend 而失败；先启动后 reload
    networks:
      - blog_net

volumes:
  rustfs_data:
  pgdata:
  redis_data:

networks:
  blog_net:
    driver: bridge
